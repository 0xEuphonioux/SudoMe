#!/bin/bash
# SudoMe Helper Revoke Wrapper — separate executable for pkexec path matching.
# Polkit distinguishes grant (auth_self) from revoke (yes) by exec.path.
# This wrapper forwards all arguments to sudome-helper (GUI already includes "revoke").
exec /usr/lib/sudome/sudome-helper "$@"
